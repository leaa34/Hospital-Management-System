# Hospital-Management-System
A Java application that manages hospital patients using Object-Oriented Programming (OOP). It uses a PriorityQueue to automatically sort patients by medical urgency using a custom Comparator.
